# Phase 3J-1 TCI Server — Verification Matrix

**Scope:** verify the Thetis TCI server port (Phase 3J-1) for byte-for-byte parity on commands, notifications, init burst, and 12 compatibility flags. Plus integration smoke against three real TCI clients.

**Upstream reference:** Thetis `v2.10.3.13` @ `501e3f51` — `Project Files/Source/Console/TCIServer.cs`. AetherSDR transport pattern reference @ `0cd4559`.

> **Auto-generated note (mid-PR):** the Commands / Notifications / Init Burst / Compat Flags sections are auto-generated by `scripts/gen-tci-matrix-readme.py` from `tests/data/tci/matrix.csv` at end-of-epic (Phase 25 of the implementation plan). **DO NOT edit those sections by hand mid-PR** — your changes will be overwritten when the script runs. Add new test rows by editing `tests/data/tci/matrix.csv` and re-running the matrix runner. The Integration Smoke section is hand-maintained.

---

## Commands

*Auto-generated from `matrix.csv`. Each row is one Thetis command behavior — set or query — with the input the runner sends, the expected synchronous response, the expected broadcast notification(s) drained from the post-handler queue, and the Thetis source cite the row was ported from.*

| Command | Input | Expected Response | Expected Notifications | Thetis Cite | Notes |
|---|---|---|---|---|---|
| *(populated at end-of-epic by `gen-tci-matrix-readme.py`)* | | | | | |

---

## Notifications

*Auto-generated from the same CSV by filtering rows whose `expected_notifications` column is non-empty. Lists each notification surface, its trigger, and its wire form.*

| Notification | Trigger | Wire Form | Thetis Cite |
|---|---|---|---|
| *(populated at end-of-epic)* | | | |

---

## Init Burst

*Auto-generated from `tests/data/tci/init_burst_anan_g2_rx1.txt` (the captured-or-synthetic golden file from Phase 4 Task 4.3). Lists every line emitted in the post-connect init burst, in order, with its source cite. Includes the `if:1,1,...;` typo divergence from Thetis (design doc §7.1).*

| # | Line | Thetis Cite | Notes |
|---|---|---|---|
| *(populated at end-of-epic)* | | | |

---

## Compat Flags

*Auto-generated. Each of the 12 compatibility flags (Phase 5) and the wire effect it gates.*

| Flag (AppSettings key) | Default | Effect Gated | Thetis Cite |
|---|---|---|---|
| *(populated at end-of-epic)* | | | |

---

## Integration Smoke (hand-maintained)

End-of-epic gate before the PR can ship. Real-client connection tests against the running server.

| # | Client | Platform | Behavior under test | Sign-off |
|---|---|---|---|---|
| 1 | N1MM Logger+ | Windows | Connect → retune via spot-click → mode change → MOX assert returns to RX cleanly | ☐ |
| 2 | Log4OM | Windows | Connect → retune → live sensor update visible on Log4OM band-map | ☐ |
| 3 | RUMlog-TCI | macOS | Connect → retune from log entry → basic RX sensor display | ☐ |

For each row, capture: NereusSDR build hash, client version, radio model, sample rate, any unexpected wire-trace deviations. Attach pcap (or `wscat -c` transcript) if anything diverges from the matrix.

---

## Sign-off

Pre-merge checklist. Tick each line in the PR body before requesting review.

- [ ] All Commands rows pass under `ctest -R '^tst_tci_matrix_runner$'`.
- [ ] All Notifications rows verified via the same runner.
- [ ] Init Burst golden file matches `buildInitBurst()` byte-for-byte for strict-protocol prefixes (loose match for `device:` / `vfo:` radio-identifying fields).
- [ ] All 12 Compat Flags wired and matrix rows green.
- [ ] Integration Smoke: at least 1 of 3 client rows signed off (3-of-3 preferred). RUMlog-TCI may SKIP if no macOS bench available; document in the PR body.
- [ ] Pre-commit / CI hooks green against the full branch:
  ```bash
  python3 scripts/verify-inline-tag-preservation.py
  python3 scripts/verify-thetis-headers.py
  python3 scripts/check-new-ports.py
  ```
- [ ] Full test suite green: `ctest --test-dir build --output-on-failure`.
